{
  "id":"type:Integer",
  "userId":"${cart.userId}",
  "date":"${cart.date}",
  "products":[
    {
      "productId":"${product.productId}",
      "quantity":"${product.quantity}"
    },
    {
      "productId":"${product.productId}",
      "quantity":"${product.quantity}"
    }
  ]
}